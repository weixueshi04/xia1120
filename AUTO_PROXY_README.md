# 自动代理功能说明

## 🎯 功能概述

自动代理功能**仅在Web环境下**启动，用于解决浏览器的跨域访问问题。

**重要说明：**
- ✅ **Web浏览器** - 自动启动代理（解决CORS跨域问题）
- ❌ **手机APP** - 不启动代理（直接调用API，无跨域限制）

## ✨ 主要特性

1. **自动启动** - 应用启动时自动检测并启动代理
2. **健康监控** - 每30秒自动检查代理健康状态
3. **自动重启** - 代理异常时自动尝试重启（最多3次）
4. **状态监控** - 提供可视化界面查看代理状态
5. **手动控制** - 支持手动启动/停止代理

## 📋 前置要求

### 必须安装 Node.js

代理服务基于Node.js运行，请确保已安装Node.js：

**Windows:**
```bash
# 检查是否已安装
node --version

# 如果未安装，访问官网下载：
# https://nodejs.org/
```

**Mac/Linux:**
```bash
# 检查是否已安装
node --version

# 使用包管理器安装
# Mac: brew install node
# Ubuntu: sudo apt install nodejs npm
```

### 代理目录结构

确保项目中存在以下目录和文件：

```
xiaapp/
├── baidu-proxy/
│   ├── server.js          # 代理服务器代码
│   ├── package.json       # Node.js依赖配置
│   ├── start.bat          # Windows启动脚本
│   └── node_modules/      # Node.js依赖包
```

## 🚀 使用方法

### 自动模式（推荐）

**Web环境（Chrome/Firefox等）：**
应用启动时会自动：
1. 检测当前是Web环境
2. 检测代理是否已运行
3. 如果未运行，自动启动代理服务
4. 验证代理健康状态
5. 开始定期健康检查

**移动端环境（Android/iOS）：**
- 跳过代理启动
- 直接调用百度API
- 无需任何代理服务

**无需任何手动操作！**

### 手动控制

如果需要手动控制代理：

1. 点击顶部工具栏的 **路由器图标** 📡
2. 进入"代理服务状态"界面
3. 可以查看状态、启动、停止代理

## 📊 代理状态界面

### 状态信息

- **运行状态** - 代理是否正在运行
- **健康检查** - 代理是否响应正常
- **重启次数** - 自动重启的次数
- **代理地址** - 代理服务的URL（http://localhost:3000）

### 操作按钮

- **启动代理** - 手动启动代理服务
- **停止代理** - 手动停止代理服务
- **刷新状态** - 立即检查代理状态

## 🔧 工作原理

### 1. 启动流程

```
应用启动
    ↓
检测运行环境
    ↓
是Web环境？ ──→ 否（移动端）──→ 跳过代理，直接调用API
    ↓ 是（Web）
初始化ProxyManager
    ↓
检查代理是否运行 ──→ 是 ──→ 开始健康监控
    ↓ 否
检查Node.js是否安装
    ↓
启动Node.js代理服务
    ↓
等待2秒
    ↓
验证代理健康状态
    ↓
开始定期健康监控（每30秒）
```

### 2. 健康监控

```
每30秒执行一次
    ↓
发送健康检查请求 (GET /health)
    ↓
检查响应状态
    ↓
如果失败 ──→ 尝试重启（最多3次）
    ↓
如果成功 ──→ 继续监控
```

### 3. 代理转发

```
Flutter应用
    ↓
发送请求到 http://localhost:3000/api/baidu/...
    ↓
Node.js代理服务
    ↓
转发到百度API (https://qianfan.baidubce.com/...)
    ↓
返回响应给Flutter应用
```

## 🐛 故障排除

### 问题1：代理启动失败

**可能原因：**
- Node.js未安装
- 端口3000被占用
- baidu-proxy目录不存在

**解决方法：**
1. 检查Node.js安装：`node --version`
2. 检查端口占用：`netstat -ano | findstr :3000`（Windows）
3. 确认baidu-proxy目录存在

### 问题2：代理健康检查失败

**可能原因：**
- 代理进程崩溃
- 网络问题
- 防火墙阻止

**解决方法：**
1. 进入"代理状态"界面
2. 点击"停止代理"
3. 等待2秒
4. 点击"启动代理"

### 问题3：百度API调用失败

**可能原因：**
- 代理未运行
- API密钥错误
- 网络连接问题

**解决方法：**
1. 检查代理状态（应该显示"运行正常"）
2. 检查API配置（appId和bearerToken）
3. 查看日志输出

## 📝 日志说明

应用会输出详细的日志信息：

```
[INFO] 初始化代理管理器...
[INFO] 检测到Node.js版本: v18.17.0
[INFO] 🚀 正在启动百度API代理服务...
[DEBUG] [Proxy] 百度Agent代理服务器启动成功！
[INFO] ✅ 代理服务启动成功！
[INFO]    地址: http://localhost:3000
```

## 🔐 安全说明

1. **本地运行** - 代理服务仅在本地运行（localhost）
2. **不对外开放** - 默认只监听127.0.0.1，不接受外部连接
3. **API密钥** - 密钥存储在代码中，建议使用环境变量

## 📚 相关文件

### 核心文件

- `lib/services/proxy_manager.dart` - 代理管理器
- `lib/screens/proxy_status_screen.dart` - 状态监控界面
- `lib/main.dart` - 应用入口（自动启动代理）
- `baidu-proxy/server.js` - Node.js代理服务器

### 配置文件

- `baidu-proxy/package.json` - Node.js依赖
- `lib/services/baidu_api.dart` - 百度API服务（使用代理）

## 🎉 总结

自动代理功能让你无需手动启动代理服务，应用会自动处理一切！

**优点：**
- ✅ 自动化 - 无需手动操作
- ✅ 可靠 - 自动监控和重启
- ✅ 可视化 - 提供状态监控界面
- ✅ 灵活 - 支持手动控制

**现在就运行应用，体验自动代理功能吧！** 🚀

```bash
flutter run
```
